# Portal environment variables — copy to .env.local for local dev.

# Auth mode — must match backend AUTH_MODE.
# trust-header: legacy v1 identity picker (IdentifyAsDropdown). Default for rollback safety.
# gitlab-sso: GitLab OAuth2 session cookie. Enables /login page + middleware redirect.
NEXT_PUBLIC_AUTH_MODE=trust-header
